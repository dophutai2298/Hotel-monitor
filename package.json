{
  "name": "hotel-room-monitor",
  "version": "1.0.0",
  "description": "Hotel Room Monitoring System - NestJS API + React Frontend",
  "private": true,
  "scripts": {
    "install:all": "cd api && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:client\"",
    "dev:api": "cd api && npm run start:dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd api && npm run build && cd ../client && npm run build",
    "start:prod": "cd api && npm run start:prod"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
